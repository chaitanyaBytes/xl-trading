{
  "name": "xl-trading-backend",
  "private": true,
  "workspaces": [
    "packages/*",
    "server",
    "price-poller",
    "ticks-batch-uploader"
  ],
  "scripts": {
    "dev": "bun run --filter='*' dev",
    "build": "bun run --filter='*' build",
    "test": "bun run --filter='*' test"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "zod": "^4.1.3"
  }
}
